{
	"private": true,
	"workspaces": {
		"packages": [
			"libraries/*",
			"projects/*"
		],
		"nohoist": [
			"**/preact_cli/**",
			"**preact_cli**",
			"**/preact_cli",
			"preact_cli",
			"**/preact",
			"**/preact-render-to-string"
		]
	},
	"scripts": {
		"watch": "yarn workspace watch",
		"test": "yarn workspace test",
		"build": "yarn workspace build",
		"dev": "yarn workspace dev"
	},
	"dependencies": {
		"lerna": "^3.22.1",
		"typescript": "^4.1.5"
	}
}
